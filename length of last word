#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a;
	getline(cin, a);
	int count = 0;
	for (int i = a.length() - 1; i >= 0; i--)
	{
		if (a[i] != ' ')
			count++;
		else
			break;
	}
	cout << count << endl;
	return 0;
}
